/**
 * 
 * 给你六种面额1、5、10、20、50、100元的纸币，假设每种币值的数量都足够多，编写程序求组成N员（N为0-10000的非负整数）的不同组合的个数
 */
function getCount(num)
{
    var temp = [];
    temp[0] = 1;
    for(var i = 1 ; i <= num; i++)
    {

        
    }
    return temp[num]
}

